{
  "name": "itsybitsy",
  "module": "src/index.ts",
  "type": "module",
  "version": "0.1.3",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build --compile --minify --sourcemap index.ts --outfile ib"
  },
  "dependencies": {
    "@mariozechner/pi-tui": "^0.56.0"
  }
}
